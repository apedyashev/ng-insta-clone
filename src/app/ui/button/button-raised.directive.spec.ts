import { UiButtonRaisedDirective } from './button-raised.directive';

describe('ButtonRaisedDirective', () => {
  it('should create an instance', () => {
    const directive = new UiButtonRaisedDirective();
    expect(directive).toBeTruthy();
  });
});
