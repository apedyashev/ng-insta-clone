import { FullWidthBlockDirective } from './full-width-block.directive';

describe('FullWidthBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new FullWidthBlockDirective();
    expect(directive).toBeTruthy();
  });
});
